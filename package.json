{
  "name": "chittyadvocate",
  "version": "1.0.0",
  "description": "ChittyAdvocate — the sputnik. Doctrine narrative bootstrap for cross-model alignment.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
